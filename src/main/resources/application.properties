# Intentionally insecure configuration (for training only)
server.port=8080

# H2 in-memory database
spring.datasource.url=jdbc:h2:mem:webgoat;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Hardcoded secrets (training only, still present)

